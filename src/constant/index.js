const DATABASE_CONSTANTS = require("./database.constants");
const { commentStatus, allCommentStatus } = require("./comment.enum");

module.exports = {
  DATABASE_CONSTANTS,
  commentStatus,
  allCommentStatus,
};
